<UserControl x:Class="JouleadAssistantDesign.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:JouleadAssistantDesign.ViewModels"
             Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <vm:ProjectListViewModel/>
    </UserControl.DataContext>

    <DockPanel>
        <!-- 操作按钮栏 -->
        <StackPanel DockPanel.Dock="Top"
                Orientation="Horizontal"
                Margin="8">
            <Button Content="新建项目"
              Command="{Binding NewProjectCommand}"
              Margin="0,0,8,0"/>
            <Button Content="导出"
              Command="{Binding ExportProjectCommand}"
              Margin="0,0,8,0"/>
            <Button Content="删除"
              Command="{Binding DeleteProjectCommand}"/>
        </StackPanel>

        <!-- 数据表格 -->
        <DataGrid ItemsSource="{Binding Projects}"
              SelectedItem="{Binding SelectedProject}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              Margin="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="项目名称" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="创建日期"
                            Binding="{Binding CreatedDate, StringFormat=\{0:yyyy-MM-dd\}}"
                            Width="120"/>
                <DataGridTextColumn Header="项目经理" Binding="{Binding Manager}" Width="120"/>
                <DataGridTextColumn Header="交接日期"
                            Binding="{Binding HandoverDate, StringFormat=\{0:yyyy-MM-dd\}}"
                            Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>

